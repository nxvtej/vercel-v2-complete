# Local mode: serve built sites from disk (recommended for local dev)
LOCAL_OUTPUT_DIR=/path/to/vercel-v2-complete/outputs

# --- S3 mode (only when LOCAL_OUTPUT_DIR is unset) ---
BASE_PATH_URL=https://vercel-v2.s3.ap-south-1.amazonaws.com/__outputs
